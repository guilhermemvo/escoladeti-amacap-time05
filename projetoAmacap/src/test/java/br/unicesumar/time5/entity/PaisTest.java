/*package br.unicesumar.time5.entity;

import static org.fest.assertions.Assertions.assertThat;
import org.junit.Test;

/**
 *
 * @author Paulo Henrique
 */
/*public class PaisTest {

    
    public void testSet() {
        
        Pais pais = new Pais();
        
        pais.setCodigoIBGE(5555);
        assertThat(pais.getCodigoIBGE()).isEqualTo(5555);
        
        pais.setNome("Brasil");
        assertThat(pais.getNome()).isEqualTo("Brasil");
        
        pais.setSigla("BR");
        assertThat(pais.getSigla()).isEqualTo("BR");
        
        pais.setId(Long.MIN_VALUE);
        assertThat(pais.getId()).isEqualTo(Long.MIN_VALUE);
    }
}
*/